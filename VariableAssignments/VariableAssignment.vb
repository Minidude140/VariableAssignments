Option Explicit On
Option Strict On


Module VariableAssignment

    Sub Main()
        Dim fruit As String
        Dim count As Integer
        Dim price As Double
        fruit = "Apples"
        count = 5
        price = 1.99
        Console.WriteLine("I have " & count & " " & fruit)
        Console.Read()

    End Sub

End Module
